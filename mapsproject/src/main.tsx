import React from "react";
import ReactDOM from "react-dom/client";

import Map from "./modules/tracking";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <Map />
  </React.StrictMode>
);
